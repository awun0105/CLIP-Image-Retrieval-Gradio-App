# Evaluation Reports

Store real retrieval and benchmark reports here.

Reports should be generated from an indexed service and must not contain
invented metrics. If an evaluation cannot be run because the service or index is
not ready, record that as a note instead of filling in fake results.
